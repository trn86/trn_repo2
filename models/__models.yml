models: 
  - name: int_customers
    description: customers model
    columns:
      - name: customer_id
        description: customer_id is the unique key of the table
        tests:
          - unique
          - not_null
    group: intermediate_models
    access: public